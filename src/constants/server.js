/**
 * Server configuration constants
 */

const DEFAULT_PORT = 3001;

const API_CONFIG = {
    model: 'gpt-4o-mini',
    temperature: 0.7,
    max_tokens: 1000,
};

module.exports = {
    DEFAULT_PORT,
    API_CONFIG
};
